[nixpacks]
providers = ["python"]

[phases.setup]
nixPkgs = ["...", "chromium"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install chromium", "playwright install-deps chromium"]

[start]
cmd = "python main.py"
